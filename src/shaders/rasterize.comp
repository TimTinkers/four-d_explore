#version 430
#extension GL_ARB_separate_shader_objects : enable

layout(set = 0, binding = 0) uniform Dimensions {
  int width;
  int height;
} dim;

layout(binding = 1, rgba8) uniform image2D output_image;
layout(binding = 2, r32i) uniform iimage2D depth_buffer;

layout(set = 1, binding = 3) buffer InputVertices {
  vec4 pos;
} inputVertices;


void main() {
  
}
